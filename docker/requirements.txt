# pip3 install -r requirements.txt

# 其他库
requests
PyYAML > 1
Flask > 1
aiohttp >= 3.8.3
Flask-APScheduler > 1
colorlog >= 6.7.0
httpx
socksio >= 1.0.0
Flask-Cors

# ws需要插件
Flask-SocketIO==5.2.0
simple-websocket
Werkzeug
# 加密
Nuitka
ordered-set

